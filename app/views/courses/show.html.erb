<div class="flex flex-col items-center w-full h-full py-12 space-y-12">
  <div class="container flex flex-col items-center p-4 space-y-12">
    <course-info
      :course="<%= CourseSerializer.new(@course, teacher: true, lectures: true, students: true).to_json %> | camelizeKeys"
      :current-user="<%= current_user.to_json %>"
    >
    </course-info>
  </div>
</div>
